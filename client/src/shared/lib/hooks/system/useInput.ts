"use client";

import useValidate from "./forms/useValidate";

/**
 * @version 0.0.2
 *
 * @param initial - начальное значение (может быть string, number, File и т. д.).
 * @param options - настройки валидации.
 */
export default function useInput<T>(initial: T){

}