enum UserRole {
  root
  admin
  moderator
  user
}

enum AuthMethod {
  credentials
  google
  discord
  vkontakte
  telegram
  apple
  uak
}

enum TokenType {
  verification
  two_factor
  password_reset
}

enum VisibleStatus {
  draft
  published
  archived
}

enum RatingSystem {
  everyone
  teen
  mature
  adult
}

enum FileType {
  note
  image
  video
  archive
  audio
}
