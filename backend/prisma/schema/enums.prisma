enum UserRole {
  root
  admin
  moderator
  user
}

enum AuthMethod {
  credentials
  uak
}

enum VisibleStatus {
  draft
  published
  archived
}

enum RatingSystem {
  everyone
  teen
  mature
  adult
}

enum FileType {
  note
  image
  video
  archive
  audio
}
